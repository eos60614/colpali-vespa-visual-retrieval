"""
Similarity map generation module.

Note: The SimMapGenerator class in backend/colpali.py handles similarity map
generation. It remains in the backend root as a shared utility between
ingestion (embedding generation) and query (similarity maps).

Import from backend.colpali:
    from backend.colpali import SimMapGenerator
"""
