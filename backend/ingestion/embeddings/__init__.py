"""
Embedding generation module.

Note: The SimMapGenerator class in backend/colpali.py handles both
embedding generation (for ingestion) and similarity map generation (for query).
It remains in the backend root as a shared utility.
"""
