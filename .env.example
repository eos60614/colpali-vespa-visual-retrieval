# =============================================================================
# Local Vespa Setup (recommended for development)
# =============================================================================
# Just set this URL to connect to local Vespa (no auth required)
VESPA_LOCAL_URL=http://localhost:8080
VESPA_DATA_DIR=/mnt/vespa-storage

# =============================================================================
# Vespa Cloud Setup (alternative - for production)
# =============================================================================
# Option 1: Token-based authentication
# VESPA_APP_TOKEN_URL=https://your-app.vespa-cloud.com
# VESPA_CLOUD_SECRET_TOKEN=your-secret-token

# Option 2: mTLS authentication
# USE_MTLS=true
# VESPA_APP_MTLS_URL=https://your-app.vespa-cloud.com
# VESPA_CLOUD_MTLS_KEY=your-private-key-content
# VESPA_CLOUD_MTLS_CERT=your-certificate-content

# =============================================================================
# LLM Configuration (AI chat and VLM features)
# =============================================================================
# REQUIRED: LLM provider base URL (OpenAI-compatible endpoint, no fallback)
# OpenRouter: https://openrouter.ai/api/v1
# OpenAI:     https://api.openai.com/v1
# Ollama:     http://localhost:11434/v1
LLM_BASE_URL=https://openrouter.ai/api/v1

# API key - set ONE of these (the code reads OPENROUTER_API_KEY first, then OPENAI_API_KEY)
# OPENROUTER_API_KEY=your-openrouter-api-key
# OPENAI_API_KEY=your-openai-api-key

# Non-sensitive config (chat model, log level, hot reload, LLM reranking, etc.) is in ki55.toml

# =============================================================================
# Procore Database Connection (read-only)
# =============================================================================
# PostgreSQL connection string for Procore integration database
# Format: postgresql://user:password@host:port/database
# PROCORE_DATABASE_URL=postgresql://readonly_user:password@procore-db.example.com:5432/procore_int_v2

# =============================================================================
# AWS S3 Configuration (for direct file access - optional)
# =============================================================================
# Only needed if using DIRECT_S3 download strategy instead of signed URLs
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_REGION=us-east-1
# S3_BUCKET=procore-integration-files
